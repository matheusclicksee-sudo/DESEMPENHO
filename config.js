// CONFIGURAÇÃO DO SUPABASE
// Cole aqui os valores encontrados em Supabase > Project Settings > API.
// Use SOMENTE a chave anon/public (publishable). NUNCA use service_role no navegador.

window.PAVONI_CONFIG = {
  SUPABASE_URL: "COLE_AQUI_A_PROJECT_URL",
  SUPABASE_ANON_KEY: "COLE_AQUI_A_ANON_PUBLIC_KEY"
};
